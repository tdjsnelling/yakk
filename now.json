{
  "name": "yakk",
  "version": 2,
  "build": {
    "env": {
      "REACT_APP_ENV": "production"
    }
  },
  "builds": [
    { "src": "client/package.json", "use": "@now/static-build", "config": { "distDir": "build" } }
  ],
  "routes": [
    { "src": "^/(.*)", "dest": "/client/$1" }
  ]
}
